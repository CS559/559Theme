# 559Theme shortcodes

**Generated** by `tools/shortcode-docs.py` from the doc-comment header of each shortcode in `layouts/_shortcodes/`. Do not edit this file by hand — edit the shortcode's header comment and regenerate.

Header convention (top of each shortcode):

```text
{{- /*
  <name> — <one-line summary>
  usage: {{< name … >}}   …
  params: 0 = …, named: …
  [notes: …]
*/ -}}
```

51 shortcodes: 49 active, 2 deprecated/dead-weight. Deep-dives: `link` → `docs/link-shortcode.md`; course-data shortcodes (`assign-*`, `reading`, `mod*`, `page`) → `docs/data-contracts.md`; math (`math`, `displaymath`, `eqref`) → `docs/math.md`.

## Index

| shortcode | file | summary | status |
|---|---|---|---|
| [`alldrafts`](#alldrafts) | `alldrafts.html` | an <h3> "Drafts" heading followed by a list (title + link) of every |  |
| [`allpages`](#allpages) | `allpages.html` | a nested <ul> of every page in the site (ordered by title), |  |
| [`anchorlink`](#anchorlink) | `anchorlink.html` | link to a heading on the CURRENT page by its text/id. |  |
| [`assetlink`](#assetlink) | `assetlink.html` | the RelPermalink (URL string) of a global pipeline asset in assets/. |  |
| [`assign-link`](#assign-link) | `assign-link.html` | should take two (three) parameters: the key for the assignment and a list of optional modifier words, which might be due, short (more to be added) |  |
| [`assign-linkonly`](#assign-linkonly) | `assign-linkonly.html` | emit just the Canvas URL for an assignment key (no anchor tag). |  |
| [`bold-red`](#bold-red) | `bold-red.md` | wrap inner text in <span class="boldred"> (bold-red emphasis). |  |
| [`comment`](#comment) | `comment.html` | ignore - I have no idea why it doesn't put the content as an HTML comment |  |
| [`content`](#content) | `content.md` | include the CONTENT from another PAGE |  |
| [`debugurl`](#debugurl) | `debugurl.html` | DEV helper: a dimbox showing Site.BaseURL and a sample resource's |  |
| [`dimbox`](#dimbox) | `dimbox.md` | wrap inner content in <div class="dimbox"> (a dimmed callout box). |  |
| [`displaymath`](#displaymath) | `displaymath.html` | process a display equation - allows for an id to refer to it |  |
| [`draft-only`](#draft-only) | `draft-only.html` | DEAD tombstone: errorf's on ANY use (the drafts feature is gone). | dead-weight |
| [`eqref`](#eqref) | `eqref.html` | link to a numbered equation by id (numbers are registered by displaymath |  |
| [`expand-boxed`](#expand-boxed) | `expand-boxed.html` | Inspired by the "details" shortcode from the hugo book theme |  |
| [`expand`](#expand) | `expand.html` | Inspired by the "details" shortcode from the hugo book theme |  |
| [`expandinline`](#expandinline) | `expandinline.md` | include the CONTENT from another PAGE |  |
| [`figure`](#figure) | `figure.html` | modified version of Hugo's figure shortcode - I copied the text |  |
| [`genai`](#genai) | `genai.html` | a "GenAI Disclosure" callout box (a dimbox with a heading). |  |
| [`htmllink`](#htmllink) | `htmllink.md` | show an internal page's absolute URL as the visible, clickable link text. |  |
| [`includemd`](#includemd) | `includemd.md` | This includes a markdown file (without frontmatter) inline |  |
| [`inline`](#inline) | `inline.md` | Places a page inline inside of another page |  |
| [`leftpic`](#leftpic) | `leftpic.html` | float an image to the left of wrapping inner text, in a flex row. |  |
| [`link`](#link) | `link.html` | link to another page in the site by its logical path. |  |
| [`linkit`](#linkit) | `linkit.md` | make an arbitrary URL clickable, using the URL itself as the link text. |  |
| [`listtoc`](#listtoc) | `listtoc.html` | Make a table of contents for a list page - uses the titles of the pages |  |
| [`mailto`](#mailto) | `mailto.html` | an email link whose visible text is the address itself. |  |
| [`math`](#math) | `math.html` | manual math - explicitly request processing |  |
| [`mikes-notes`](#mikes-notes) | `mikes-notes.html` | basically, comment out something - but we need to refer to .Inner! |  |
| [`mini`](#mini) | `mini.md` | embed another page rendered with its "mini" view, inside a teaser box. |  |
| [`moddesc`](#moddesc) | `moddesc.html` | look up a module and give its description - provide the module number - |  |
| [`modlo`](#modlo) | `modlo.html` | look up a module and give its learning outcomes - provide the module number - |  |
| [`modname`](#modname) | `modname.html` | look up a module and give its name - provide the module number - |  |
| [`next`](#next) | `next.html` | link to the "next" page in the section (Hugo's .Page.Prev). |  |
| [`page`](#page) | `page.html` | look up a link to a page/service in the page database. |  |
| [`pages`](#pages) | `pages.md` | DEAD tombstone: errorf's on ANY use. Superseded by `link` (or `page`). | dead-weight |
| [`prev`](#prev) | `prev.html` | link to the "previous" page in the section (Hugo's .Page.Next). |  |
| [`quote`](#quote) | `quote.html` | a <blockquote> of the inner markdown, with an optional citation. |  |
| [`reading`](#reading) | `reading.html` | look up a reading and put the html in the page |  |
| [`resource-file`](#resource-file) | `resource-file.html` | just returns a link to a page resource - actually makes a line, using the name |  |
| [`resource-image`](#resource-image) | `resource-image.html` | a <figure> for an image resource, linking the full image to a fitted copy. |  |
| [`resource-link`](#resource-link) | `resource-link.html` | just returns a link to a page resource |  |
| [`resource-svg`](#resource-svg) | `resource-svg.html` | render an SVG page resource as a figure, inline SVG, highlighted source, or a link. |  |
| [`rimage`](#rimage) | `rimage.html` | This is the preferred way to include images. It supports captions, attributions, and resizing. It should work for SVG and for PNG/JPG/GIF. |  |
| [`snippet`](#snippet) | `snippet.html` | inserts a markdown snippet from assets |  |
| [`static`](#static) | `static.html` | a root-relative URL (relURL) to a file in static/ (a bare string, not a tag). |  |
| [`tableofcontents`](#tableofcontents) | `tableofcontents.html` | render the current page's table of contents inline |  |
| [`teasehtml`](#teasehtml) | `teasehtml.html` | render another page using its "summary" view, with NO wrapper div. |  |
| [`teaser`](#teaser) | `teaser.md` | embed another page's "summary" view inside <div class="teaser">. |  |
| [`tooltip`](#tooltip) | `tooltip.html` | an inline trigger that reveals a rich (markdown) tooltip on hover or |  |
| [`url`](#url) | `url.html` | Make URLS that generate absolute local links |  |

## Reference

### alldrafts

<sup>`layouts/_shortcodes/alldrafts.html`</sup>

an <h3> "Drafts" heading followed by a list (title + link) of every

```text
draft page in the site, for authoring/review. Empty if there are no drafts.
usage: {{< alldrafts >}}
params: none.
```

### allpages

<sup>`layouts/_shortcodes/allpages.html`</sup>

a nested <ul> of every page in the site (ordered by title),

```text
excluding the Tags/Categories trees. Backs the "list of all pages" page.
usage: {{< allpages >}}
params: none.
```

### anchorlink

<sup>`layouts/_shortcodes/anchorlink.html`</sup>

link to a heading on the CURRENT page by its text/id.

```text
usage: {{< anchorlink "Section Title" >}}       text = the arg
       {{< anchorlink "section-id" "Label" >}}  custom link text
params: 0 = heading text/id (anchorized for the href); 1 = link text (optional, defaults to arg 0).
```

### assetlink

<sup>`layouts/_shortcodes/assetlink.html`</sup>

the RelPermalink (URL string) of a global pipeline asset in assets/.

```text
usage: <img src="{{< assetlink "img/logo.png" >}}">
params: 0 = path under assets/. Emits a URL string, not a tag (cf. resource-image). Errors if not found.
```

### assign-link

<sup>`layouts/_shortcodes/assign-link.html`</sup>

should take two (three) parameters: the key for the assignment and a list of optional modifier words, which might be due, short (more to be added)

```text
should take two (three) parameters: the key for the assignment and a list of optional modifier words, which might be due, short (more to be added)
   the third parameter is a page link - if that's given, the canvas link is given separately

   The assigns file is in the site's data/ directory, generated by a Python
   script (SimpleCourseTools/assigns.py). Two file names are accepted:
   assignments.yaml (preferred) or assigns.yaml. Provide only one - if both
   exist it is a build error.
```

### assign-linkonly

<sup>`layouts/_shortcodes/assign-linkonly.html`</sup>

emit just the Canvas URL for an assignment key (no anchor tag).

```text
emit just the Canvas URL for an assignment key (no anchor tag).
   Data source: data/assignments.yaml (preferred) or data/assigns.yaml.
   Provide only one - if both exist it is a build error.
```

### bold-red

<sup>`layouts/_shortcodes/bold-red.md`</sup>

wrap inner text in <span class="boldred"> (bold-red emphasis).

```text
usage: {{% bold-red %}}important{{% /bold-red %}}
params: none; takes inner content.
```

### comment

<sup>`layouts/_shortcodes/comment.html`</sup>

ignore - I have no idea why it doesn't put the content as an HTML comment

```text
ignore - I have no idea why it doesn't put the content as an HTML comment
```

### content

<sup>`layouts/_shortcodes/content.md`</sup>

include the CONTENT from another PAGE

```text
include the CONTENT from another PAGE
  * (this requires things to be a findable page)
  *
  * adapted from https://discourse.gohugo.io/t/solved-remove-frontmatter-of-content-read-with-readfile/12682/10

   See also:
   https://discourse.gohugo.io/t/include-file-shortcode-for-including-files-with-shortcode-calls/16699

   but note this is an md shortcode (maybe it can be converted to an html shortcode using markdownify)

   when it comes time for headless resources, I might need
   https://discourse.gohugo.io/t/using-hugo-shortcodes-in-readfile/20161
   which talks about how to access resources from page bundles
```

### debugurl

<sup>`layouts/_shortcodes/debugurl.html`</sup>

DEV helper: a dimbox showing Site.BaseURL and a sample resource's

```text
RelPermalink/Permalink, for debugging baseURL / asset-path issues.
usage: {{< debugurl >}}
params: none.
```

### dimbox

<sup>`layouts/_shortcodes/dimbox.md`</sup>

wrap inner content in <div class="dimbox"> (a dimmed callout box).

```text
usage: {{% dimbox %}}note text{{% /dimbox %}}
params: none; takes inner (markdown) content.
```

### displaymath

<sup>`layouts/_shortcodes/displaymath.html`</sup>

process a display equation - allows for an id to refer to it

```text
process a display equation - allows for an id to refer to it
```

### draft-only 🪦 dead-weight

<sup>`layouts/_shortcodes/draft-only.html`</sup>

> **dead-weight:** kept as a tripwire to force content off it; left as errorf (not

DEAD tombstone: errorf's on ANY use (the drafts feature is gone).

```text
  warnf) on purpose. Reconsider full removal once no consumer could reference it.
```

### eqref

<sup>`layouts/_shortcodes/eqref.html`</sup>

link to a numbered equation by id (numbers are registered by displaymath

```text
in .Page.Scratch "eq_refs").
usage: {{< eqref "eq:euler" >}}
params: 0 = equation id. Renders "Equation N" (or "Equation (See below)" if not yet numbered).
```

### expand-boxed

<sup>`layouts/_shortcodes/expand-boxed.html`</sup>

Inspired by the "details" shortcode from the hugo book theme

```text
Inspired by the "details" shortcode from the hugo book theme
```

### expand

<sup>`layouts/_shortcodes/expand.html`</sup>

Inspired by the "details" shortcode from the hugo book theme

```text
Inspired by the "details" shortcode from the hugo book theme
```

### expandinline

<sup>`layouts/_shortcodes/expandinline.md`</sup>

include the CONTENT from another PAGE

```text
include the CONTENT from another PAGE
  * (this requires things to be a findable page)
  *
  * adapted from https://discourse.gohugo.io/t/solved-remove-frontmatter-of-content-read-with-readfile/12682/10

   See also:
   https://discourse.gohugo.io/t/include-file-shortcode-for-including-files-with-shortcode-calls/16699

   but note this is an md shortcode (maybe it can be converted to an html shortcode using markdownify)

   when it comes time for headless resources, I might need
   https://discourse.gohugo.io/t/using-hugo-shortcodes-in-readfile/20161
   which talks about how to access resources from page bundles
```

### figure

<sup>`layouts/_shortcodes/figure.html`</sup>

modified version of Hugo's figure shortcode - I copied the text

```text
modified version of Hugo's figure shortcode - I copied the text
    from https://raw.githubusercontent.com/gohugoio/hugo/master/tpl/tplimpl/embedded/templates/shortcodes/figure.html
    or https://github.com/gohugoio/hugo/blob/master/tpl/tplimpl/embedded/templates/shortcodes/figure.html
    on 8/24/2020

    if Hugo updates this, it will need to be updated

    additions:
    a new "rsrc" parameter that looks up the name as a page resource
       see https://discourse.gohugo.io/t/figure-shortcode-should-use-page-resources/12989/2
   but it will also search site resources

   note: the hack to skip extensions was not implemented because site resource search
   does not do matching

   you can put wildcards in the name string for rsrc (if you know its a page resource)
```

### genai

<sup>`layouts/_shortcodes/genai.html`</sup>

a "GenAI Disclosure" callout box (a dimbox with a heading).

```text
usage: {{% genai %}}Describe any generative-AI use here.{{% /genai %}}
params: none; takes inner (markdown) content.
```

### htmllink

<sup>`layouts/_shortcodes/htmllink.md`</sup>

show an internal page's absolute URL as the visible, clickable link text.

```text
usage: {{% htmllink "some/page" %}}   ->   [https://…/some/page/](https://…/some/page/)
params: 0 = logical page path (resolved with `ref`). (cf. link = uses the page title as the text.)
```

### includemd

<sup>`layouts/_shortcodes/includemd.md`</sup>

This includes a markdown file (without frontmatter) inline

```text
This includes a markdown file (without frontmatter) inline
  * DO NOT USE THIS FOR PAGES (it's only for things without frontmatter)
```

### inline

<sup>`layouts/_shortcodes/inline.md`</sup>

Places a page inline inside of another page

```text
Places a page inline inside of another page
  * note that it must be a page (and not just a markdown fragment)
  * it is processed with the "inline" template
```

### leftpic

<sup>`layouts/_shortcodes/leftpic.html`</sup>

float an image to the left of wrapping inner text, in a flex row.

```text
usage: {{% leftpic rsrc="pic.jpg" %}}text beside the picture{{% /leftpic %}}
params (named): one of rsrc (page-resource glob) | asset (global asset) | url (raw URL) | nopic;
  width (px, default 200); gutter (px, default 10); alt; title; topmargin/bottommargin (default 1rem).
  Inner content is the right-hand text column. Errors if none of rsrc/asset/url/nopic is given.
```

### link

<sup>`layouts/_shortcodes/link.html`</sup>

link to another page in the site by its logical path.

```text
Usage:
  {{< link "some/page" >}}                          one bare positional arg = the page
  {{< link page="some/page" >}}                     named form (page is required)
  {{< link page="some/page" anchor="Section" >}}    link to a heading on that page
  {{< link page="some/page" text="click me" >}}     custom link text
  `link` is accepted as an alias for `page`.

Defaults (override only with named params):
  - text: the linked page's title; when an anchor is given and no text is
    set, it becomes "Title (Anchor)".
  - anchor: none.
  - Explicit text is used verbatim; anchor still sets the URL #fragment.

Removed: two positional arguments. The old shortcode took the anchor as a
second positional arg (and the old `lnk` took the text there), which was
ambiguous — hence named params now. One positional arg (the page) is fine.
```

### linkit

<sup>`layouts/_shortcodes/linkit.md`</sup>

make an arbitrary URL clickable, using the URL itself as the link text.

```text
usage: {{% linkit "https://example.com/x" %}}   ->   [https://example.com/x](https://example.com/x)
params: 0 = URL, used verbatim as both text and href. For external URLs; does no page lookup.
```

### listtoc

<sup>`layouts/_shortcodes/listtoc.html`</sup>

Make a table of contents for a list page - uses the titles of the pages

```text
Make a table of contents for a list page - uses the titles of the pages
   note: this requires that the summary sets the IDs of the headings
```

### mailto

<sup>`layouts/_shortcodes/mailto.html`</sup>

an email link whose visible text is the address itself.

```text
usage: {{< mailto "someone@example.com" >}}
params: 0 = email address (used as both href and text).
```

### math

<sup>`layouts/_shortcodes/math.html`</sup>

manual math - explicitly request processing

```text
manual math - explicitly request processing
clunkier than the more automated things - but happens at compile time
The variant-fix partial rewrites KaTeX's mathvariant attrs to real Unicode
glyphs so bold/script/etc. render in Chromium (see docs/math-bold-research/).
```

### mikes-notes

<sup>`layouts/_shortcodes/mikes-notes.html`</sup>

basically, comment out something - but we need to refer to .Inner!

```text
basically, comment out something - but we need to refer to .Inner!
```

### mini

<sup>`layouts/_shortcodes/mini.md`</sup>

embed another page rendered with its "mini" view, inside a teaser box.

```text
usage: {{% mini "/path/to/page" %}}
params: 0 = logical page path. (cf. teaser = "summary" view; inline = full content.)
```

### moddesc

<sup>`layouts/_shortcodes/moddesc.html`</sup>

look up a module and give its description - provide the module number -

```text
look up a module and give its description - provide the module number -
```

### modlo

<sup>`layouts/_shortcodes/modlo.html`</sup>

look up a module and give its learning outcomes - provide the module number -

```text
look up a module and give its learning outcomes - provide the module number -
```

### modname

<sup>`layouts/_shortcodes/modname.html`</sup>

look up a module and give its name - provide the module number -

```text
look up a module and give its name - provide the module number -
```

### next

<sup>`layouts/_shortcodes/next.html`</sup>

link to the "next" page in the section (Hugo's .Page.Prev).

```text
usage: {{< next >}}            text = "Next: <title>"
       {{< next "Custom" >}}   custom link text
params: 0 = link text (optional). Errors if there is no next page.
```

### page

<sup>`layouts/_shortcodes/page.html`</sup>

look up a link to a page/service in the page database.

```text
look up a link to a page/service in the page database.

   Data source (provide exactly one - both is a build error):
     * assets/pages.csv  (columns: key,name,url[,heading]) - primary
     * data/pages.yaml   (map of key -> {url, name})       - fallback

   usage:
     {{< page "regrade-form" >}}            link, text = entry's name
     {{< page "regrade-form" "alt text" >}} link with custom text
```

### pages 🪦 dead-weight

<sup>`layouts/_shortcodes/pages.md`</sup>

> **dead-weight:** kept as a tripwire pointing authors to the replacement; left as

DEAD tombstone: errorf's on ANY use. Superseded by `link` (or `page`).

```text
  errorf (not warnf) on purpose. Original impl parked in deprecated/pages.md.
```

### prev

<sup>`layouts/_shortcodes/prev.html`</sup>

link to the "previous" page in the section (Hugo's .Page.Next).

```text
usage: {{< prev >}}            text = "Prev: <title>"
       {{< prev "Custom" >}}   custom link text
params: 0 = link text (optional). Renders nothing if there is no previous page.
```

### quote

<sup>`layouts/_shortcodes/quote.html`</sup>

a <blockquote> of the inner markdown, with an optional citation.

```text
usage: {{% quote "Author, Source" %}}the quoted text{{% /quote %}}
params: 0 = citation (optional, markdown, shown in <cite>); inner = the quote body.
```

### reading

<sup>`layouts/_shortcodes/reading.html`</sup>

look up a reading and put the html in the page

```text
look up a reading and put the html in the page
       usage: {{< reading "reading-id" "assignment" >}}
       where "reading-id" is the key in data/readings.yaml
       and "type" is the kind of reading
       "required" "optional" "alternate" etc.
```

### resource-file

<sup>`layouts/_shortcodes/resource-file.html`</sup>

just returns a link to a page resource - actually makes a line, using the name

```text
just returns a link to a page resource - actually makes a line, using the name
```

### resource-image

<sup>`layouts/_shortcodes/resource-image.html`</sup>

a <figure> for an image resource, linking the full image to a fitted copy.

```text
usage: {{< resource-image src="photo.jpg" size="300x300" alt="…" >}}
params (named): src (required; page resource or global asset); size (default 300x300, uses .Fit);
  alt; class. Errors if src is not found. Prefer `rimage` for new content.
```

### resource-link

<sup>`layouts/_shortcodes/resource-link.html`</sup>

just returns a link to a page resource

```text
just returns a link to a page resource
```

### resource-svg

<sup>`layouts/_shortcodes/resource-svg.html`</sup>

render an SVG page resource as a figure, inline SVG, highlighted source, or a link.

```text
usage: {{< resource-svg src="diagram.svg" >}}            figure (default)
       {{< resource-svg src="d.svg" inline="true" >}}     inline the SVG markup
       {{< resource-svg src="d.svg" highlight="true" >}}  show the SVG source (highlighted)
       {{< resource-svg src="d.svg" link="true" >}}       just a link
params (named): src (required); one of inline / highlight / link; alt; class.
```

### rimage

<sup>`layouts/_shortcodes/rimage.html`</sup>

This is the preferred way to include images. It supports captions, attributions, and resizing. It should work for SVG and for PNG/JPG/GIF.

```text
 This is the preferred way to include images. It supports captions, attributions, and resizing. It should work for SVG and for PNG/JPG/GIF.

this is meant to take the place of resource-image
it is not a drop-in for resrouce image, because it uses WIDTH not SIZE
we need to use WIDTH since we need it for the caption

it takes the place of figure (in that it allows for a caption), but allows for the resource finding and re-sizing of resource-image (it doesn't resize SVGs - it just sets the width)

this whole thing was caused by the need to have the caption sized to the width of the image (since the captions look stupid otherwise)

the argugements are:
src - the reource name
width - target width (default 300) - height is auto
alt - alt text (default is the resource name)
class - class for the figure (default is "")
caption - caption for the image (default is "")
attr - attribution for the image (default is "")
attrlink - link for the attribution (default is "")
```

### snippet

<sup>`layouts/_shortcodes/snippet.html`</sup>

inserts a markdown snippet from assets

```text
 inserts a markdown snippet from assets

    first parameter is a md file in the /assets/snippets directory
    second parameter (optional) is an html tag to wrap the result in

* requires the asset to be a plain markdown (not a page with frontmatter)
* naively looks in /assets/snippets
```

### static

<sup>`layouts/_shortcodes/static.html`</sup>

a root-relative URL (relURL) to a file in static/ (a bare string, not a tag).

```text
usage: <img src="{{< static "img/logo.png" >}}">
params: 0 = path under static/. (cf. assetlink = for assets/ pipeline resources.)
```

### tableofcontents

<sup>`layouts/_shortcodes/tableofcontents.html`</sup>

render the current page's table of contents inline

```text
(Hugo .Page.TableOfContents), titled "Page Contents".
usage: {{< tableofcontents >}}
params: none.
```

### teasehtml

<sup>`layouts/_shortcodes/teasehtml.html`</sup>

render another page using its "summary" view, with NO wrapper div.

```text
usage: {{< teasehtml "/path/to/page" >}}
params: 0 = logical page path. (cf. teaser = same but wrapped in <div class="teaser">.)
```

### teaser

<sup>`layouts/_shortcodes/teaser.md`</sup>

embed another page's "summary" view inside <div class="teaser">.

```text
usage: {{% teaser "/path/to/page" %}}
params: 0 = logical page path. (cf. teasehtml = unwrapped; mini = the "mini" view.)
```

### tooltip

<sup>`layouts/_shortcodes/tooltip.html`</sup>

an inline trigger that reveals a rich (markdown) tooltip on hover or

```text
keyboard focus. More than a plain title= attribute: the content is markdown.
usage: {{% tooltip element="term" %}}explanatory *markdown*{{% /tooltip %}}
       {{% tooltip element="term" title="Heading" width="20em" %}}…{{% /tooltip %}}
params (named): element (REQUIRED — the trigger text, markdown); title (optional
  bold heading line); width / maxwidth (optional CSS sizes for the bubble).
notes: styling is self-contained in assets/css/tooltip.scss (load via a site's
  customCss). Reveals on :hover AND :focus-within (keyboard/touch accessible).
  Replaced the old 140KB hint.css dependency — the former `color` param (and any
  other hint.css option) is no longer supported and will warn if used.
```

### url

<sup>`layouts/_shortcodes/url.html`</sup>

Make URLS that generate absolute local links

```text
Make URLS that generate absolute local links
```

